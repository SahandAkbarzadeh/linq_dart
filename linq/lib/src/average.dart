num average<num>(
  Iterable<num> source,
) {
  assert(false, "Not implemented");
}

num average2<TSource>(
  Iterable<TSource> source, {
  num Function(TSource a) predicate,
}) {
  assert(false, "Not implemented");
}
