num sum<num>(
  Iterable<num> source,
) {
  assert(false, "Not implemented");
}

TResult sum2<TSource, TResult>(
  Iterable<TSource> source,
  TResult Function(TSource a) selector,
) {
  assert(false, "Not implemented");
}
