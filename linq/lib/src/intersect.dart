import 'package:linq/src/interfaces.dart';

Iterable<TSource> intersect<TSource>(
  Iterable<TSource> first,
  Iterable<TSource> second, {
  EqualityComparer<TSource> comparer = null,
}) {
  assert(false, "not implemented");
}
