TSource elementAt<TSource>(
  Iterable<TSource> source,
  int index,
) {
  assert(false, "not implemented");
}

TSource elementAtOrDefault<TSource>(
  Iterable<TSource> source,
  int index, {
  TSource defaultValue = null,
}) {
  assert(false, "not implemented");
}
