Iterable<TSource> ofType<TSource>(
  Iterable<TSource> source,
) {
  assert(false, "Not implemented");
}

Iterable<TResult> cast<TResult>(
  Iterable source,
) {
  assert(false, "Not implemented");
}
